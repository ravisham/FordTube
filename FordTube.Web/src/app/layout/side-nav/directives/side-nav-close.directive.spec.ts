import { SideNavCloseDirective } from './side-nav-close.directive';

describe('SideNavCloseDirective', () => {
  it('should create an instance', () => {
    const directive = new SideNavCloseDirective();
    expect(directive).toBeTruthy();
  });
});
