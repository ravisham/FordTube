/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { HeaderSearchCloseDirective } from './header-search-close.directive';

describe('Directive: HeaderSearchClose', () => {
  it('should create an instance', () => {
    const directive = new HeaderSearchCloseDirective();
    expect(directive).toBeTruthy();
  });
});
